---
"@effect-grpc/effect-grpc": patch
---

Target `effect@4.0.0-rc.115` (from rc.112). The pinned peer dependency
moves with it, so consumers need to upgrade `effect` and
`@effect/platform-node` together.
